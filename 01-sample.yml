- hosts: all
  tasks:
    - name: Print Hello
      ansible.builtin.debug:
        msg: Hello
- name: Demo of Play
  hosts: all
  tasks:
    - name: Print Bye
      ansible.builtin.debug:
        msg: Bye

- name: Facts in ansible
  hosts: all
  tasks:
    ansible.builtin.debug:
      msg: {{ansible_hostname}}